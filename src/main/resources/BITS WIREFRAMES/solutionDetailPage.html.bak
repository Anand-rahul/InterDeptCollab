<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BITS HUB - Solution Details</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Poppins', sans-serif; background-color: #f4f6f9; }
        .header-section { background: linear-gradient(135deg, #007bff, #0056b3); color: white; padding: 50px; text-align: center; border-radius: 0 0 50px 50px; }
        .header-section h1 { font-size: 2.2rem; font-weight: 600; margin-bottom: 10px; }
        .solution-info { display: flex; justify-content: space-between; margin-top: 20px; }
        .solution-info span { font-size: 14px; font-weight: 500; }
        .section-title { font-size: 22px; font-weight: bold; margin-bottom: 10px; }
        .solution-details { background: white; border-radius: 10px; padding: 20px; box-shadow: 0px 4px 12px rgba(0,0,0,0.1); }
        .faq-item { border-bottom: 1px solid #ddd; padding: 10px 0; }
        .cta-button { background: linear-gradient(135deg, #007bff, #0056b3); color: white; padding: 12px 18px; border-radius: 8px; border: none; cursor: pointer; transition: 0.3s; font-weight: 500; }
        .cta-button:hover { background: linear-gradient(135deg, #0056b3, #003f7d); }
        .floating-chat { position: fixed; bottom: 20px; right: 20px; background: #007bff; color: white; padding: 15px; border-radius: 50%; box-shadow: 0px 4px 10px rgba(0,0,0,0.2); cursor: pointer; font-size: 22px; }
        .floating-chat:hover { background: #0056b3; }
        .sticky-submit { position: fixed; bottom: 10px; left: 50%; transform: translateX(-50%); background: #28a745; color: white; padding: 12px 20px; border-radius: 8px; box-shadow: 0px 4px 10px rgba(0,0,0,0.2); cursor: pointer; font-size: 16px; font-weight: 600; }
        .sticky-submit:hover { background: #218838; }
    </style>
</head>
<body>

    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-light b
